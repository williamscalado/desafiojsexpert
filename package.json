{
  "name": "desafiojsexpert",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "repository": "https://github.com/williamscalado/desafiojsexpert.git",
  "author": "Williams Calado <williamscalado@gmail.com>",
  "license": "MIT",
  "scripts": {
    "dev": "nodemon server/index.js",
    "test": "set NODE_OPTIONS=--experimental-vm-modules && npx jest --runInBand --no-cache",
    "test:live": "set NODE_OPTIONS=--experimental-vm-modules && npx jest --runInBand --no-cache --watchAll",
    "test:coverage": "set NODE_OPTIONS=--experimental-vm-modules && npx jest --runInBand --no-cache --coverage test/unit"
  },
  "engines": {
    "node": "17"
  },
  "dependencies": {
    "pino": "7.8.0",
    "pino-pretty": "^7.5.3"
  },
  "devDependencies": {
    "jest": "^27.5.1"
  }
}
